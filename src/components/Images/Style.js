import styled from "styled-components";

export const ImageMapa = styled.Image`

width: 100%;

`;

export const ImageDoutor = styled(ImageMapa) ``

export const ImagePerfil = styled(ImageMapa) `

height: 105%; 

`
